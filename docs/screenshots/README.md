# Deployment evidence

Screenshots in this directory are sanitized portfolio evidence. They must not contain passwords, secret keys, private keys, administrator credentials, database contents, or private customer information.

- `https-certificate.png` — trusted Let's Encrypt certificate on the live BuildCore subdomain
